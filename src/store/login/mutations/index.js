export default {
  login (state, admin) {
    state.admin = admin
  },
  // push (state) {
  //   state.list.push(Math.random())
  // },
  // remove(state, { index }){
  //   const arr = state.list
  //   state.list.splice(index, 1)
  // },
  // pop(state) {
  //   state.list.pop()
  // }
}