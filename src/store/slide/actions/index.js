export default {
  collapse (context) {
    context.commit('collapse')
  },
  // push (context) {
  //   context.commit('push')
  // },
  // remove(context, params) {
  //   context.commit('remove', params)
  // },
  // pop(context) {
  //   context.commit('pop')
  // }
}